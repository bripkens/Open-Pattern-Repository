<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @pattern.title %>
</p>

<p>
  <b>Slug:</b>
  <%= @pattern.slug %>
</p>

<p>
  <b>Description:</b>
  <%= @pattern.description %>
</p>

<p>
  <b>Outgoing relationships:</b>
  <ul>
    <% @pattern.outgoing_relationships.each do |relationship| %>
    <li>
      &lt;&lt;<%= relationship.relationship_type.title %>&gt;&gt;<br />
      Target: <%= link_to relationship.target.title, relationship.target %><br />
      <%= relationship.description %>
    </li>
    <% end %>
  </ul>
</p>

<p>
  <b>Incoming relationships:</b>
  <ul>
    <% @pattern.incoming_relationships.each do |relationship| %>
    <li>
      &lt;&lt;<%= relationship.relationship_type.title %>&gt;&gt;<br />
      Source: <%= link_to relationship.source.title, relationship.source %><br />
      <%= relationship.description %>
    </li>
    <% end %>
  </ul>
</p>


<%= link_to 'Edit', edit_pattern_path(@pattern) %> |
<%= link_to 'Back', patterns_path %>
